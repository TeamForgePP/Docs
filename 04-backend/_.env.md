
файл окружения, содержащий переменные, необходимые для запуска контейнеров (например, данные доступа, порты, пути). Используется `docker-compose` при старте сервисов.

```env
# --- postgres ---
POSTGRES_USERNAME=postgres
POSTGRES_DB=teamforge
POSTGRES_PORT=5432
POSTGRES_HOST=postgres
POSTGRES_PASSWORD=postgres

# --- minio (пароль >= 8 символов) ---
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=12345678

# --- app ---
REDIS_URL=redis://redis:6379/0
```
